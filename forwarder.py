from nbconvert import export
parentAddress = Hash(default_value=0)

# Contract that will forward any incoming Ether to the creator of the contract
def Forwarder():
    # Address to which any funds sent to this contract will be forwarded

    # Create the contract, and sets the destination address to that of the creator
    def Forwarder():
    return True

    # Modifier that will execute internal code block only if the sender is the parent address
    def ModonlyParent():
     return True


    # Default function; Gets called when Ether is deposited, and forwards it to the parent address
    @export
    def payable():
     return True


    # throws on failure

    # Fire off the deposited event if we can forward it

    # Execute a token transfer of the full balance from the forwarder token to the parent address
    # @param tokenContractAddress the address of the erc20 token contract
    @export
    def flushTokens(tokenContractAddress: str):
     return True


    # It is possible that funds were sent to this address before the contract was deployed.
    # We 
    #  flush those funds to the parent address.
    @export
    def flush():
     # throws on failure
     return True
